import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const LiveProjects = () => {
  return (
    <View style={{marginBottom: 163}}>
      <Text>sani</Text>
    </View>
  );
};

export default LiveProjects;

const styles = StyleSheet.create({});
